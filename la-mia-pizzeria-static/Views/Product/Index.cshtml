@{
    ViewData["Title"] = "Home Page";
    Layout = "_MyLayout";
}

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Pizzeria da Samu</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
</head>
<body>
    <div class="container">
        <div class="d-flex justify-content-center align-items-center vh-100"><img style="width:80%; height:80%;" src="~/svg/Da Samu.svg" /></div>

        <div class="row">
            <h1 class="mb-2">Menù </h1>
            <p class="mb-4"> Il nostro impasto è preparato con ingredienti di alta qualità, selezionati con cura, e lievitato lentamente per oltre 48 ore. Questo processo garantisce una consistenza leggera e croccante, con una fragranza irresistibile, che esalta ogni boccone e rende ogni pizza un'esperienza unica e indimenticabile.</p>
            <hr />
            @foreach (var product in Model)
            {
                <div class="col-md-4">
                    <div class="pizza">
                        <img src="@Url.Content(product.Image)" />
                        <div class="d-flex">
                            <h4 class="pt-3">@product.Name</h4>
                            <a href="@Url.Action("Details", "Product", new { id = product.Id })"><img style="width:20px; height:20px; margin: 10px;" src="~/img/eye.png" /></a>
                        </div>

                        <p>@product.Description</p>
                        <div class="ms-auto">@product.Price €</div>
                        
                    </div>
                </div>
            }
        </div>
    </div>
    <style>
        .pizza {
            display: flex;
            flex-direction: column;
            height: 100%;
            padding: 15px;
        }

            .pizza img {
                width: 150px;
                height: 150px;
            }

            .pizza p {
                flex-grow: 1;
            }
    </style>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
